name: sigv4
description: Library for signing AWS requests with Signature Version 4
author: arnemolland <arneolaim@gmail.com>

version: 0.0.1

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  crypto: ^2.0.6
  meta: ^1.1.7
  intl: ^0.15.7
  http: ^0.12.0+1